### Наибольшее произведение двух чисел

Дан список, заполненный произвольными целыми числами. 
Найдите в этом списке два числа, произведение которых максимально. Выведите эти числа в порядке неубывания.
Список содержит не менее двух элементов. Числа подобраны так, что ответ однозначен.
Решение должно иметь сложность O(n), где _n_ - размер списка.
```py
numbers = list(map(int, input().split())) 
numbers.sort()
prod_begin = numbers[0] * numbers[1] 
prod_end = numbers[-1] * numbers [-2]
if prod_begin > prod_end:           #в отсортированном списке проверяем, что больше, произведение двух первых чисел (отрицательных) или двух последних
    print(numbers[0], numbers[1])
else:
    print(numbers[-1], numbers[-2])
```
